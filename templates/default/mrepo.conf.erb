### Configuration file for mrepo

[main]
srcdir = <%= node['repo']['mrepo']['srcdir']%>
wwwdir = /var/www/mrepo
confdir = /etc/mrepo.conf.d
arch = i386 

mailto = root@localhost
smtp-server = localhost

