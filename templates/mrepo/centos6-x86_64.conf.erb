[centos6]
name = CentOS Server $release ($arch)
release = 6
arch = x86_64
metadata = repomd yum repoview

# Main OS repos
os = rsync://rsync.mirrorservice.org/mirror.centos.org/$release/os/$arch/
updates = rsync://rsync.mirrorservice.org/mirror.centos.org/$release/updates/$arch/
